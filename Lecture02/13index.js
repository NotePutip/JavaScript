const message ="Hello";
const place = "World";

// loogging out a string
console.log('Hello, World!');

// using substitutions
console.log('Hello, %s!',place);

//using a string lieral
console.log(`${message}, ${place}!`);