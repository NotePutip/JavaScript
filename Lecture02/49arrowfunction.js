const sum = (firstParam, secondParam) => {
    return firstParam + secondParam;
}
console.log(sum(2,5));

const printHello = () =>{
    console.log("Heelo");
};
printHello();

const checkWeight = (weight) => {
    console.log(`Baggage weight : ${weight} kilograms.`);
};
checkWeight(25);

const multply = (a, b) => a * b;
console.log(multply(2,30));